#include <stdio.h>
#include <math.h>

int main()
{


    printf("a to the powre b is %f\n", pow(6, 3));

    int c;
    int d;
    printf("enter the value of c is %d");
    scanf("%d",&c);
    printf("enter the value of d is %d");
    scanf("%d",&d);
    printf("c to the power d is %f", pow(c, d));

    return 0;
}