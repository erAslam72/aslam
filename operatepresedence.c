#include <stdio.h>
int main()
{
    int x = 8;
    int y = 5;

    printf("the value of 3*x-8*y is %d\n", 3 * x - 8 * y);
    int a = 1,
        b = 2;
        printf("the value of 9*a+15*b-6*a*b+a+b is %d\n",-1*a*b);
        int c=2;
        float d=2.5;
        printf("the value of c*c+d*d-8*c-9*d is %f",c*c+d*d-8*c-9*d);
    return 0;
}